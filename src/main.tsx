import React from "react";
import ReactDOM from "react-dom/client";
import TravelBuddyApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <TravelBuddyApp />
  </React.StrictMode>
);
